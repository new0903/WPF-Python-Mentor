<Page x:Class="WpfAppPythone.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfAppPythone.Pages"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:templates="clr-namespace:WpfAppPythone.Templates"
      xmlns:viewModels="clr-namespace:WpfAppPythone.ViewModels"
      xmlns:models="clr-namespace:WpfAppPythone.AppData.Models"
      xmlns:system="clr-namespace:System;assembly=mscorlib"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">
    <Page.DataContext>
        <viewModels:MainViewModel />
    </Page.DataContext>


    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Button.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.ToggleButton.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Shadows.xaml" />
            </ResourceDictionary.MergedDictionaries>


        </ResourceDictionary>
    </Page.Resources>



    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <materialDesign:Card Grid.Row="0" Margin="10">
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <templates:SectionChart ChartQuests="{Binding Quests}" ChartSections="{Binding Sections}"/>

                <Border Grid.Column="1" BorderThickness="1,0,0,0" BorderBrush="#E0E0E0">
                   
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text=" ВашиОчки"/>
                            <materialDesign:PackIcon Kind="Star" Foreground="Gold"/>
                            <TextBlock Text="{Binding Points}" Margin="10,0"/>
                        </StackPanel>
                        <Button Margin="10,20" Content="Купить в магазине зелья" Command="{Binding GoToMarketCommand}"/>
                    </StackPanel>
                </Border>

            </Grid>

        </materialDesign:Card >

        <ScrollViewer x:Name="MainScrollViewer"
                        Grid.Row="1"
                        Grid.ColumnSpan="2"
                        materialDesign:ScrollViewerAssist.IsAutoHideEnabled="True"
                        >

            <ContentControl Margin="16" >
                <StackPanel Orientation="Vertical">
                    <ItemsControl ItemsSource="{Binding Sections}">
                        <ItemsControl.ItemTemplate>

                            <DataTemplate>
                                <templates:SectionCard SelectSectionCard="{Binding DataContext.SelectSectionCommand, RelativeSource={RelativeSource AncestorType=Page}}"  />

                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

            </ContentControl>
        </ScrollViewer>
    </Grid>
</Page>
<!--DataContext="{Binding RelativeSource={RelativeSource Self}}"-->

<!--<templates:SectionChart Grid.Column="0" AllSection="{Binding Sections}" AllQuest="{Binding Quests}"/>-->
<!--<templates:SectionCard/>-->
<!--, RelativeSource={RelativeSource AncestorType=viewModels:MainViewModel}-->